Restriction due to importing rehype and remark plugins to `next.config.js`.

- No typescript.
- No ESM yet. `unified` and `remark-parse` are not listed in `package.json` to use versions `@mdx-js/mdx` depends on.
