import dynamic from 'next/dynamic';
import { Seo } from '@c/organisms/Seo';
import { UlCol } from '@c/atoms/UlCol';
import { SchoolTable } from '@c/organisms/SchoolTable';
import CostTable from '@c/organisms/CostTable';
import { ComparePageMeta } from '@d';
import { getForex } from '@l/forex';
import { AtomStore, withRecoil } from '@l/recoil';
import { ForexStateKey, defaultForexState } from '@s/forex';

export const TuitionBarChart = dynamic(
  () => import('@c/organisms/TuitionBarChart'),
);

export const getStaticProps = async(context) => {
  const atomStore = new AtomStore();
  console.log(process.env.NODE_ENV)
  const forex = process.env.NODE_ENV === 'production'
    ? await getForex('USD', 'JPY')
    : defaultForexState;
  atomStore.setAtom(ForexStateKey, forex);
  return atomStore.with({
    revalidate: 60 * 12
  });
}

<Seo
  title={ComparePageMeta.title}
  description={ComparePageMeta.description}
  openGraph={{
    type: 'article',
    title: ComparePageMeta.title,
    description: ComparePageMeta.description,
  }}
/>


# BYU、BYUH、BYUIの比較


## 学校データ

<SchoolTable />


## 入学に必要な英語力

[英語の勉強方法#入学に必要な英語力](/tips/apply#入学に必要な英語力)へ。


## 学費

<TuitionBarChart className='w-full h-72 md:h-80 lg:h-96' />


## 1学期の費用

<CostTable />
